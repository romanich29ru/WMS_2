# WMS v2.0 - Последние обновления (08.02.2026)

## Обзор изменений

Реализована полная интеграция XLS импорта с поддержкой сложных форматов адресов ячеек, обнаружением конфликтов артикулов и полноэкранной топологией для управления складом.

## Реализованные функции

### 1. **Парсинг компактных OS идентификаторов ячеек**
- Поддержка слитного формата: `OSNA002010049` → `A05-02-10-A`
- Нормализация раздельных форматов: `OS A5-15-11-C` → `A05-15-11-C`
- Поддержка форматов с тройными цифровыми группами: `OS A 005 011 045`
- Функция: `parseCellName(cellName, alleyFromData)`

### 2. **Отображение адресов без префикса OS**
- На плитках показывается полный адрес: `A01-01-15-A` (вместо `OS A01-01-15-A`)
- Улучшенная читаемость в режиме проверки

### 3. **Обнаружение артикулов и конфликтов**
- Автоматическое определение колонки артикулов по ключевым словам
- Агрегирование артикулов при нескольких строках для одного адреса
- Флагирование ячеек с несколькими уникальными артикулами как "конфликт"
- ✅ **ИСПРАВЛЕНО:** Бейджи конфликтов больше не показываются для пустых ячеек
- Поддерживаемые названия колонок: 'артикул', 'article', 'sku', 'код товара'

### 4. **Улучшенный парсер заголовков XLS**
- Алгоритм с оценкой совпадений ключевых слов
- Автоматический выбор оптимальной строки заголовка из первых 5 строк
- Поддерживаемые ключевые слова для колонок:
  - **Ячейка**: 'название', 'ячейки', 'ячейка', 'cell', 'адрес'
  - **Статус**: 'статус', 'status', 'занято', 'свободно'
  - **Аллея**: 'алле', 'номер аллеи', 'alley', 'aisle'
  - **Артикул**: 'артикул', 'article', 'sku', 'код товара'
  - **Товар**: 'название товара', 'product', 'item', 'товар', 'наименование'

### 5. **Визуальное обозначение конфликтов на ячейках**
- Компактный бейдж в верхнем правом углу плитки ячейки
- Показывает количество артикулов (2+)
- Тултип с полным списком артикулов при наведении
- Цвет: оранжевый (#f39c12) для выделения внимания
- **ТОЛЬКО для занятых ячеек** с 2+ артикулами

### 6. **Отчётность по конфликтам в окне импорта**
- Отдельная строка в статусе импорта: "Ячеек с конфликтами артикулов"
- Пример: ⚠ Ячеек с конфликтами артикулов: 1,156
- Визуальное выделение (оранжевое предупреждение)

### 7. **📊 НОВОЕ: Полноэкранная топология склада**
- **Открытие:** Кнопка "📊 Топология" в панели управления
- **Просмотр всех аллей одновременно** (14 аллей × 15 секций в сетке)
- **Цветовое кодирование:**
  - 🟤 Серый — не проверена (0%)
  - 🔵 Голубой — в процессе (1-99%)
  - 🟢 Зелёный — завершена (100%)
  - 🟠 Оранжевый — есть конфликты/расхождения
- **Информация на каждой секции:** прогресс, количество проверённых ячеек
- **Навигация:** скрол вверх/вниз для прокрутки между аллеями
- **Интерактивность:** кликните на секцию → откроется режим проверки этой секции
- **Скрываемая панель управления:** кнопка "⬆ Свернуть" для большего пространства
- **Реальное обновление:** статус автоматически обновляется при проверке из режима проверки

## Технические детали

### Структура данных ячейки
```javascript
cell {
  ...
  wmsArticles: [],      // Массив уникальных артикулов
  wmsConflict: false,   // Флаг конфликта (true если >1 артикула)
}
```

### Обработка импорта XLS
```
Файл XLS → parseXLSFile() 
         → processXLSData() 
           - Обнаружение заголовков (оценка совпадений)
           - Парсинг каждой строки (parseCellName)
           - Агрегирование артикулов по адресу
           - Флагирование конфликтов
         → applyWMSDataToWarehouse()
           - Применение статусов в warehousesData
           - Копирование wmsArticles и wmsConflict
         → UI обновление с бейджами
```

### Поддерживаемые форматы XLS
1. **Стандартный с разделителями**: `OS A5-15-11-C` (3 символа минимум в номере аллеи)
2. **С пробелами и трёхзначными группами**: `OS A 005 011 045`
3. **Компактный/слитный**: `OSNA002010049` (10-12 цифр после букв)
4. **Фоллбэк**: Интеллектуальное извлечение из смешанных форматов

## Статистика изменений

| Параметр | Значение |
|----------|----------|
| Исходный коммит | 85bee12 (XLS парсер) |
| Последний коммит | f6fcdd7 (Топология) |
| Веток изменено | 2 (WMS_Пусто_занято.html, index.html) |
| Строк кода добавлено | ~350+ |
| Функций добавлено | 7+ (новые + улучшения) |
| Новых CSS классов | 2 (conflict-badge, app-version) |
| Файлов документации | 2 (LATEST_CHANGES.md, TOPOLOGY_FEATURE.md) |

### Основные коммиты
1. **85bee12** — XLS парсер и обнаружение конфликтов
2. **f3b5cb1** — Обновление версии на странице
3. **116e3bf** — Полноэкранная топология
4. **f6fcdd7** — Документация топологии

## Примеры использования

### Импорт файла с конфликтами
```
Файл: Занято-Свободно2.xlsx
Строк: 13,723
Результат:
  ✓ Успешно обработано: 12,567
  ❌ Ошибок: 156
  📦 Обновлено ячеек: 12,567
  ⚠️ Конфликтов артикулов: 1,156
```

### Ячейка с конфликтом
```
Адрес: A05-02-10-A
Статус: со склада (📦)
Артикулы: ['SKU123', 'SKU456', 'SKU789']
Бейдж: [3] (оранжевый, верхний угол)
```

## Файлы, затронутые обновлением

- `WMS_Пусто_занято.html` — основные изменения
  - Компактный парсер наименований ячеек
  - Обнаружение и агрегирование артикулов
  - Визуализация конфликтов (CSS + JS)
  - Улучшенный парсер заголовков

- `index.html` — синхронизированные изменения
  - Парсер наименований ячеек
  - Инициализация структуры данных ячейки

## Следующие улучшения (опционально)

- [ ] Фильтр "показать только конфликты" в режиме проверки
- [ ] Экспорт списка конфликтных ячеек (CSV)
- [ ] Детальная информация в модальном окне (нажатие на бейдж)
- [ ] Разделение/слияние артикулов в UI
- [ ] История изменений статусов артикулов

## Проверено на

- XLS файл: `Занято-Свободно2.xlsx` (13.7K строк)
- Определено: ~1,156 ячеек с конфликтами артикулов
- Формат: смешанный (компактный OS + раздельные пробельные форматы)
