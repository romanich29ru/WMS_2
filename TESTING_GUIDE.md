# 🧪 Инструкция по тестированию множественных артикулов

## ⚡ Быстрый старт

### 1. Откройте тестовую страницу в браузере:
```
https://wms-2-brown.vercel.app/TEST_MULTIPLE_ARTICLES.html
```

Или если работаете локально:
```
file:///путь/к/проекту/TEST_MULTIPLE_ARTICLES.html
```

### 2. Нажмите на одну из кнопок для загрузки тестовых данных:
- **✅ Правильный XLS** - Данные без ошибок
- **❌ Множественные артикулы** - Несколько артикулов в одной ячейке (ОШИБКА)
- **⚠️ Смешанные ошибки** - Комбинация правильных и ошибочных данных
- **⬜ Пустые ячейки** - Нормальные и пустые ячейки
- **🔀 Разные разделители** - Артикулы с ; | , разделителями

### 3. Результаты появятся в разделе "Результаты валидации":
- 📊 Статистика обработанных ячеек
- 📈 Количество ошибок
- 📋 Детальный отчет с ошибками

---

## 🔬 Что тестируется

### Сценарий 1: Правильные данные
```
| A01-01-01-A | occupied | SKU-001 | 5 |
| A01-01-02-A | occupied | SKU-002 | 3 |
| A01-01-03-A | empty    |         |   |

✅ РЕЗУЛЬТАТ: Все ячейки корректны
- Обработано: 3
- OK: 3
- Ошибок: 0
```

### Сценарий 2: Множественные артикулы (ОШИБКА)
```
| A01-01-01-A | occupied | SKU-001; SKU-002 | 5; 3 |

❌ ОШИБКА: В ячейке 2 артикулов вместо 1
- Обработано: 1
- OK: 0
- Ошибок: 1
- Множественные артикулы: 1
```

### Сценарий 3: Разные разделители
```
| A01-01-01-A | occupied | SKU-001; SKU-002      | 5; 3     |
| A01-01-02-A | occupied | SKU-003|SKU-004       | 2|1      |
| A01-01-03-A | occupied | SKU-005, SKU-006      | 4, 2     |

❌ Система ловит ошибки независимо от разделителя:
- Множественные артикулы обнаружены: 3
- Разделители обработаны: ; | ,
```

---

## 📊 Интерпретация результатов

### Статистика

| Метрика | Описание | Пример |
|---------|---------|--------|
| **Обработано** | Всего строк в XLS файле | 10 |
| **✅ OK** | Ячейки без ошибок | 8 |
| **❌ Ошибок** | Ячейки с проблемами | 2 |
| **⚠️ Множественные** | Ячейки с несколькими артикулами | 2 |

### Интерпретация ошибок

**Если `Множественные = 0`:**
```
✅ ХОРОШО - XLS файл готов к использованию
Каждой ячейке назначен ровно один артикул
```

**Если `Множественные > 0`:**
```
❌ ОШИБКА - Требуется исправление XLS
Некоторые ячейки имеют несколько артикулов
Нужно переделать формат файла
```

---

## 🔧 Как исправить ошибки

### Проблема: Несколько артикулов в одной ячейке

**❌ НЕПРАВИЛЬНО:**
```excel
| A01-01-01-A | occupied | SKU-001; SKU-002 | 5; 3 |
```

**✅ ПРАВИЛЬНО:**
```excel
| A01-01-01-A | occupied | SKU-001 | 5 |
| A01-01-02-A | occupied | SKU-002 | 3 |
```

### Шаги исправления:

1. **Откройте XLS файл в Excel**
2. **Найдите ячейку с ошибкой** (в консоли показывается ID)
3. **Разделите артикулы:**
   - Один артикул остается в исходной строке
   - Другие артикулы переносятся в новые строки
   - Для каждого артикула создается отдельная строка в XLS
4. **Сохраните файл**
5. **Загрузите заново** и проверьте ошибки

---

## 📝 Примеры консоли

### Пример 1: Правильные данные
```
[10:30:45] 📥 Загрузка: Правильный XLS (без ошибок)...
[10:30:45] ✅ Валидация завершена: 3 ячеек обработано
[10:30:45] ✅ Без ошибок: 3
[10:30:45] ❌ Ошибок: 0
```

### Пример 2: С ошибками
```
[10:31:20] 📥 Загрузка: Множественные артикулы (ОШИБКА)...
[10:31:20] ⚠️ Валидация завершена: 3 ячеек обработано
[10:31:20] ❌ С ошибками множественных артикулов: 2
[10:31:20] 📋 Ошибки:
[10:31:20]   - A01-01-01-A: SKU-001, SKU-002
[10:31:20]   - A01-01-02-A: SKU-003, SKU-004, SKU-005
```

---

## 🎯 Критерии успешного тестирования

- ✅ Система распознает правильные данные
- ✅ Система обнаруживает множественные артикулы
- ✅ Система показывает корректное количество ошибок
- ✅ Консоль выводит полную информацию об ошибках
- ✅ Статистика обновляется после каждой загрузки
- ✅ Различные разделители (;|,) обрабатываются одинаково

---

## 🚀 Тестирование на реальных данных

### Вариант 1: Интегрировать в index.html

Если хотите проверить на реальных данных в основной приложении:

```html
<!-- Добавьте в index.html перед </body> -->
<script src="/modules/xls-articles-validator.js"></script>

<!-- При загрузке XLS файла используйте: -->
<script>
const results = XLSArticlesValidator.parseAndValidate(xlsData);

// Проверить ошибки
if (results.statistics.cellsWithMultipleArticles > 0) {
    console.error('❌ Найдены ошибки:', results.multipleArticlesErrors);
    // Показать модальное окно с ошибками
}
</script>
```

### Вариант 2: Использовать quickstart.js

Функции автоматически проверяют ошибки при загрузке:

```javascript
// Эта функция уже вызывает валидатор
handleXLSFileUpload(file);

// Ошибки будут показаны в:
// 1. Консоли браузера (console.log)
// 2. Модальном окне с деталями
// 3. Отчете со статистикой
```

---

## 📈 Метрики и статистика

### При загрузке XLS система собирает:

```javascript
{
  statistics: {
    totalRows: 100,              // Всего строк в файле
    cellsProcessed: 95,          // Успешно обработано
    cellsOk: 90,                 // Ячейки без ошибок
    cellsWithErrors: 5,          // Ячейки с ошибками
    cellsWithMultipleArticles: 5,// Множественные артикулы
    occupiedCells: 70,           // Занятые ячейки
    emptyCells: 25,              // Пустые ячейки
    parseErrors: 0               // Ошибки парсинга
  }
}
```

---

## 🐛 Отладка

### Если что-то не работает:

1. **Откройте DevTools** (F12)
2. **Перейдите на вкладку Console**
3. **Вы увидите:**
   - Детальные ошибки парсинга
   - Список ячеек с проблемами
   - Статистику обработки

### Команды для отладки:

```javascript
// Вызвать валидатор напрямую
XLSArticlesValidator.parseAndValidate(data)

// Посмотреть результаты
const results = XLSArticlesValidator.parseAndValidate(data);
console.log(results.statistics);
console.log(results.multipleArticlesErrors);

// Генерировать отчет
const report = XLSArticlesValidator.generateErrorReport(results);
console.log(report.summary);
```

---

## ✅ Чек-лист завершения

После прохождения тестов убедитесь:

- [ ] Открыли TEST_MULTIPLE_ARTICLES.html
- [ ] Протестировали все 6 сценариев
- [ ] Консоль показывает результаты
- [ ] Статистика обновляется корректно
- [ ] Ошибки определяются правильно
- [ ] Готовы загрузить реальный XLS файл

---

## 📞 Справка

**Вопрос:** Как узнать, есть ли ошибки в моем XLS файле?
**Ответ:** Загрузите его на TEST_MULTIPLE_ARTICLES.html - система сразу покажет все ошибки.

**Вопрос:** Что делать если система не ловит ошибки?
**Ответ:** Проверьте, что файл подключен: `<script src="/modules/xls-articles-validator.js"></script>`

**Вопрос:** Можно ли автоматически исправить ошибки?
**Ответ:** Нет, нужно вручную переделать XLS файл. Система только показывает проблемы.

**Вопрос:** Какой формат разделителя использовать?
**Ответ:** Система поддерживает ; | , - используйте любой, результат будет одинаковый.
