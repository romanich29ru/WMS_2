# 📊 Гайд: Ваш формат XLS файла

## ✅ Поддерживаемая структура

Валидатор теперь настроен на ваш точный формат XLS файла:

| Столбец |名称 | Описание | Пример |
|---------|------|---------|--------|
| **A** | Зона | Категория ячейки | Буфер - не активные |
| **B** | ШК_Ячейки | Barcode | OSNA002010049 |
| **C** | **Название Ячейки** ✓ | Адрес ячейки | OS NA 002 010 049 |
| **D** | **Статус** ✓ | Свободна/Занята | Свободна |
| **E** | Номер Аллеи | Аллея | NA |
| **F** | Ярусность | Уровень | (пусто) |
| **G** | Глубина | Глубина | (пусто) |
| **H** | ID_Ячейки | ID | 5460 |
| **I** | **Артикулы** ✓ | SKU товара | (пусто для свободных) |

---

## 🔄 Обработка данных

### Столбец C: Название Ячейки
Система автоматически преобразует форматы:

```
OS NA 002 010 049  →  CELL-002-010-049
OS A01-01-01-A    →  A01-01-01-A
A01-01-01-A       →  A01-01-01-A
A01 01 01 A       →  A01-01-01-A
A01010A           →  A01-01-01-A
```

### Столбец D: Статус
Система распознает:

```
✅ ЗАНЯТО:
- "Занята"
- "occupied"
- "true"
- "1"

✅ СВОБОДНО:
- "Свободна"
- "empty"
- "false"
- "0"
```

### Столбец I: Артикулы
**Правильная работа:**

```
✅ ПРАВИЛЬНО (один артикул):
- SKU-001
- 12345
- PROD-ABC

✅ СВОБОДНО (пусто):
- (оставить пустым)
- пусто
- —
```

**ОШИБКА (несколько артикулов):**

```
❌ ОШИБКА - множественные артикулы:
- SKU-001; SKU-002
- SKU-001|SKU-002
- SKU-001, SKU-002
```

---

## 📋 Примеры правильного формата

### Пример 1: Свободная ячейка

```
| Зона | ШК_Ячейки | Название Ячейки | Статус | ... | ID_Ячейки | Артикулы |
|------|-----------|-----------------|--------|-----|-----------|----------|
| Буфер| OSNA002010049 | OS NA 002 010 049 | Свободна| ... | 5460 | (пусто) |
```

**Результат:**
```
✅ Ячейка: CELL-002-010-049
✅ Статус: empty (свободна)
✅ Артикулов: 0
✅ OK - нет ошибок
```

### Пример 2: Занятая ячейка с одним артикулом

```
| Зона | ШК_Ячейки | Название Ячейки | Статус | ... | ID_Ячейки | Артикулы |
|------|-----------|-----------------|--------|-----|-----------|----------|
| Буфер| OSNA002020049 | OS NA 002 020 049 | Занята | ... | 5911 | SKU-12345 |
```

**Результат:**
```
✅ Ячейка: CELL-002-020-049
✅ Статус: occupied (занята)
✅ Артикулов: 1 (SKU-12345)
✅ OK - нет ошибок
```

### Пример 3: ОШИБКА - Несколько артикулов в одной ячейке

```
| Зона | ШК_Ячейки | Название Ячейки | Статус | ... | ID_Ячейки | Артикулы |
|------|-----------|-----------------|--------|-----|-----------|----------|
| Буфер| OSNA002030049 | OS NA 002 030 049 | Занята | ... | 5962 | SKU-001; SKU-002 |
```

**Результат:**
```
❌ ОШИБКА: Ячейка CELL-002-030-049
❌ Найдено 2 артикула: SKU-001, SKU-002
❌ В одной ячейке может быть только 1 артикул!
```

---

## 📥 Как загрузить XLS файл

### 1. В основном приложении (index.html)
```javascript
// Нажать кнопку "Загрузить XLS"
// Выбрать ваш файл с данными
// Система автоматически:
// 1. Распарсит столбцы C, D, I
// 2. Проверит на ошибки множественных артикулов
// 3. Покажет результаты
// 4. Применит данные к виртуальному складу
```

### 2. На тестовой странице (TEST_MULTIPLE_ARTICLES.html)
```javascript
// Нажать "❌ Множественные артикулы"
// Система загрузит пример с вашей структурой
// Покажет обработку и ошибки
```

---

## 📊 Результаты валидации

После загрузки файла вы увидите отчет:

### Структура результата:
```javascript
{
  cellsData: {
    "CELL-002-010-049": {
      cellId: "CELL-002-010-049",
      systemStatus: "empty",         // empty или occupied
      expectedArticles: [],          // массив артикулов
      articlesCount: 0,              // количество артикулов
      hasMultipleArticles: false,    // множественные ошибки?
      articlesError: null            // сообщение об ошибке
    }
  },
  
  statistics: {
    totalRows: 100,                  // всего строк в XLS
    cellsProcessed: 95,              // обработано
    cellsOk: 90,                     // без ошибок
    cellsWithErrors: 5,              // с ошибками
    cellsWithMultipleArticles: 5,    // множественные артикулы
    occupiedCells: 70,               // занятых ячеек
    emptyCells: 25                   // свободных ячеек
  },
  
  multipleArticlesErrors: [
    {
      cellId: "CELL-002-030-049",
      articlesCount: 2,
      articles: [{sku: "SKU-001", qty: 1}, {sku: "SKU-002", qty: 1}],
      message: "Ячейка содержит 2 артикулов: SKU-001, SKU-002"
    }
  ],
  
  errors: [...]                      // все ошибки включая множественные
}
```

---

## ✅ Проверка перед загрузкой

Убедитесь что в вашем XLS файле:

- [ ] Столбец C заполнен названиями ячеек (OS NA XXX XXX XXX)
- [ ] Столбец D содержит статус (Свободна/Занята)
- [ ] Столбец I содержит только ОДИН артикул (если заполнен)
- [ ] Нет пустых строк между данными
- [ ] Первая строка содержит заголовки

---

## 🔧 Технические детали

### Парсинг названия ячейки (столбец C)

```javascript
// Система автоматически:
1. Удаляет префиксы "OS", "NA"
2. Заменяет пробелы на дефисы
3. Приводит к стандартному формату

// Примеры преобразования:
"OS NA 002 010 049" → "CELL-002-010-049"
"OS NA 002-010-049" → "CELL-002-010-049"
"OS NA002010049"    → "CELL-002-010-049"
```

### Парсинг статуса (столбец D)

```javascript
// Система ищет ключевые слова:

// ДЛЯ "ЗАНЯТО":
- заня (буквы 1-4 слова "Занята")
- occu (for "occupied")
- true, 1

// ДЛЯ "СВОБОДНО":
- свобод (for "Свободна")
- empt (for "empty")
- пусто, пуст
- false, 0

// Если неясно → default "empty"
```

### Парсинг артикулов (столбец I)

```javascript
// Система разделяет по любым из этих символов:
; (точка с запятой)
| (вертикальная черта)
, (запятая)

// ПРИМЕРЫ:
"SKU-001" → [SKU-001]
"SKU-001; SKU-002" → [SKU-001, SKU-002] ❌ ОШИБКА!
"SKU-001|SKU-002" → [SKU-001, SKU-002] ❌ ОШИБКА!
"SKU-001,SKU-002" → [SKU-001, SKU-002] ❌ ОШИБКА!

// ЛОГИКА ОШИБКИ:
if (articles.length > 1) {
  → ОШИБКА: В ячейке N артикулов вместо 1
}
```

---

## 🎯 Интеграция в код

### JavaScript - как использовать валидатор

```javascript
// 1. Загрузить XLS файл
const xlsFile = document.getElementById('xls-input').files[0];

// 2. Прочитать файл
const data = await readXLSFile(xlsFile);

// 3. Валидировать с вашим форматом
const results = XLSArticlesValidator.parseAndValidate(data);

// 4. Проверить результаты
console.log('Обработано:', results.statistics.cellsProcessed);
console.log('Ошибок:', results.statistics.cellsWithMultipleArticles);

// 5. Применить данные
applyWMSDataToWarehouse(results.cellsData);
```

---

## 📞 Найти ошибку в отчете

Если в статистике `cellsWithMultipleArticles > 0`:

```javascript
// Получить список всех ошибок
const errors = results.multipleArticlesErrors;

// Для каждой ошибки
for (const error of errors) {
  console.log(`Ячейка ${error.cellId}:`);
  console.log(`  Артикулы: ${error.articles.map(a => a.sku).join(', ')}`);
  console.log(`  Количество: ${error.articlesCount}`);
}
```

**У вас будет вывод:**
```
Ячейка CELL-002-030-049:
  Артикулы: SKU-001, SKU-002
  Количество: 2
```

Найдите эту ячейку в вашем XLS файле и исправьте - оставьте только один артикул!

---

## 🚀 Готово!

Система готова к использованию с вашим форматом XLS файла.

Откройте приложение и загрузите свой файл:
👉 https://wms-2-brown.vercel.app/

Все данные будут обработаны по вашей структуре! 🎉
